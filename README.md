# Frontend Template

## 🚀 Быстрый старт

```bash
# Создать из шаблона:
git clone https://github.com/Estepa08/frontend-template.git my-project
cd my-project
rm -rf .git
git init
npm install

# Или использовать как шаблон GitHub:
# 1. Нажмите "Use this template" на странице шаблона
# 2. Создайте новый репозиторий
# 3. git clone <your-repo> && cd <your-repo> && npm install
